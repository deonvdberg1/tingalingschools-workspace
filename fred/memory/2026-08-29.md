# 2026-08-29

## 🕐 14:34 Heartbeat Sweep — post power-off checkup

### Status: ALL SYSTEMS UP ✅
- API (3001): ok, db connected
- Portal (3000): app.autoeffortless.com serving bundle index-rzgHpwfZ.js (DocChat v4.2, current)
- Ollama: qwen3-embedding:0.6b loaded (11434)
- All LaunchAgents auto-started after boot; all ports listening (3000/3001/8092/8099)

### 🔧 Fix 1: store.autoeffortless.com serving wrong content (BUG)
- Site-server (8092, unified: marketing at / + store SPA at /apps) had been running since 08-27
- Old `com.autoeffortless.storefront` vite-preview agent (same port 8092) was superseded but still crash-looping: "Port 8092 is already in use" ×10 in log
- Root cause of wrong content: site-server served marketing at root for ALL hosts, so store.autoeffortless.com showed the marketing homepage instead of the store
- **Fix:** site-server now host-routes — store.autoeffortless.com → store SPA at root (assets served, SPA fallback), all other hosts → marketing site at /
- **Fix:** disabled stale `com.autoeffortless.storefront` LaunchAgent (launchctl disable + bootout, plist kept for rollback) — no more crash loop

### Verified after fix
- autoeffortless.com → marketing site ✅
- store.autoeffortless.com → store SPA ("AutoEffortless — Effortless Business Apps") ✅ public tunnel confirmed
- /apps on main site still serves store SPA ✅
- Store SPA asset 200 ✅

### ⚠️ Memory search tool still down
- In-session `memory_search` reports index metadata missing (embedding provider/model mismatch)
- Was fixed at CLI level on 08-27 (index --force with qwen3-embedding) but in-session tool still stale — needs gateway restart or re-index to fully resolve

## Status
- All green, both fixes verified live. Nothing needs Mr D.
- Pending (Mr D side, unchanged): Cloudflare cache purge, UPS purchase, Phase 2 Stripe signal, client intros, parent opt-in campaign, Ethernet plug-in
- 14:37 heartbeat: API 200, Ollama up (qwen3-embedding), all 6 tunnels up, ports 3000/3001/8092/8099 listening, app.autoeffortless.com serving index-rzgHpwfZ.js (v4.2 bundle). All systems nominal.
