# 2026-08-28 — DocChat v4.2 shipped (day of)

## What got done today (all LIVE)
- **10:01** Fixed DocChat crash "Can't find variable: AeIcon" (missing import) → bundle index-Cq7Eiwro.js
- **12:30** DocChat v3: cover thumbnails (pdf.js), real PDF page viewer, notebook per doc, search-all-documents modal
- **13:10** DocChat v4: viewer crash fixed (pdfjs 4.8.69 — v6 used Map.getOrInsertComputed/URL.parse not in Mr D's Safari), sidebar hide/show + full-width workspace, drag & drop + multi-file upload, all file types + QuickLook cover thumbs, highlight→note annotations
- **13:40** v4.1: thumb backfill for existing docs (startup + lazy + client retry), highlights in notebook with jump-to-highlight, full-viewport layout (no wasted space)
- **13:55** v4.2: multi-select docs + chat across selection (docIds[]), real OCR for scanned PDFs (poppler+tesseract), notebook ✕ delete for notes+highlights, draggable column dividers (persisted)
- **13:59** Resume point written — power off

## Deploys today
- Portal bundles: index-Cq7Eiwro.js → index-g_hrvpr2.js → index-xAWWSeF2.js → index-y88swdkm.js → index-rzgHpwfZ.js (live)
- Backend: docchat-routes.js (OCR, multi-doc chat, thumbs, annotations), server.js (requireAuth ?token=)

## Notes
- OCR deps installed: poppler, tesseract (Homebrew). Absolute paths used (LaunchAgent PATH lacks /opt/homebrew/bin)
- All test users cleaned; 4 purchases / 8 users remain (Mr D's own)
- Infrastructure all on LaunchAgents — auto-start after power-off
